# Sandbox 镜像（占位 / 规划中）
#
# 目标：每会话隔离环境，含 sky130 PDK + EDA 工具链 + tools/ 工具包。
# 详见 ../ARCHITECTURE.md §5。
#
# TODO:
#   - 基础镜像（确定 OS / Python 版本）
#   - 安装 sky130 PDK，设置 PDK_ROOT
#   - 安装 rustspice / mylayout（待确认来源与安装方式）
#   - 安装 magic / klayout / netgen
#   - 安装 tools/ 各工具包（含 circuit_optimizer 及其引擎依赖）
#   - 资源限额 / 无出网 由运行时（容器编排）施加，不在镜像内

# FROM <base>
# ENV PDK_ROOT=/pdk
# ...（待填充）
