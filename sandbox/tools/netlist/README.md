# netlist —— 网表模板渲染

把 `../../../templates/` 里的参数化拓扑模板，按给定参数（W/L/电流等）渲染成
具体 SPICE 网表。供优化器（每次迭代填新参数）和 agent 调用。

职责：
- 解析模板的参数占位符与端口角色元数据
- 用一组参数值渲染出可仿真的网表
- 暴露模板的可调变量及建议边界（供优化器 / B2 初值）

> 状态：规划中，待网表格式确认。
