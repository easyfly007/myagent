# frontend —— React + TypeScript（后期）

浏览器端 Web GUI（见 `../ARCHITECTURE.md` §6.5）。双栏布局：

- 左：对话区（消息流 + EDA 流水线工具卡片）
- 右：只读工作区（波形 / 版图 / DRC / LVS / PEX / 后仿报告 tab）
- 顶栏：模型选择器、下载产物、用户头像

设计原型见 `../design/mockup.html`（可直接在浏览器打开）。

> 状态：规划中。后端 API 成型后再正式开发；以后用 Tauri 打包为桌面 app。
